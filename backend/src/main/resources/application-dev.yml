spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/storage_control
    username: scuser
    password: changeme_in_production
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true              # helpful to see SQL while developing locally

  devtools:
    restart:
      enabled: true